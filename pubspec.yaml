name: media_viewer
description: "A new Flutter package project."
version: 0.0.1
homepage: https://github.com/Taskulu/media_viewer

environment:
  sdk: '>=3.2.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0